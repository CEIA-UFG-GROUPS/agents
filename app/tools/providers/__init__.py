"""Provider abstraction for flight/hotel search.

The demo ships simulated providers; real Google Flights / Skyscanner / hotel
providers can be registered later behind these same interfaces without touching
agent or orchestrator code. See specs/tools.md (Provider Abstraction).
"""
